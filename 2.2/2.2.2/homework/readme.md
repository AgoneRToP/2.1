# Запустите сервер: `node index.js`

# Сервер будет доступен по адресу: `http://localhost:9876`

# Методы:
```
GET /tasks	Получить список всех задач

GET	/tasks/:id	Получить одну задачу по её ID

DELETE	/tasks/:id	Удалить задачу по ID из файла
```